---
const trustBadges = [
  { icon: '✨', text: 'ทำสดใหม่' },
  { icon: '📅', text: 'รับออเดอร์ล่วงหน้า' },
  { icon: '🏢', text: 'เหมาะสำหรับประชุม-สัมมนา' },
];
---

<section class="relative min-h-screen flex items-center pt-20 overflow-hidden">
  <!-- Decorative elements -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <!-- Top right circle -->
    <div class="absolute -top-20 -right-20 w-80 h-80 bg-accent/20 rounded-full blur-3xl"></div>
    <!-- Bottom left circle -->
    <div class="absolute -bottom-40 -left-40 w-96 h-96 bg-secondary/10 rounded-full blur-3xl"></div>
    <!-- Small decorative shapes -->
    <svg class="absolute top-32 left-10 w-8 h-8 text-cute-accent/40 animate-float" viewBox="0 0 24 24" fill="currentColor">
      <circle cx="12" cy="12" r="10" />
    </svg>
    <svg class="absolute top-48 right-20 w-6 h-6 text-accent/50 animate-float" style="animation-delay: 0.5s" viewBox="0 0 24 24" fill="currentColor">
      <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" />
    </svg>
    <svg class="absolute bottom-32 left-1/4 w-10 h-10 text-secondary/30 animate-float" style="animation-delay: 1s" viewBox="0 0 24 24" fill="currentColor">
      <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z" />
    </svg>
  </div>

  <div class="relative max-w-6xl mx-auto px-4 py-12 md:py-20">
    <div class="text-center max-w-3xl mx-auto">
      <!-- Main headline -->
      <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold text-primary leading-tight mb-4 animate-fade-in-up">
        ขนมไทยทำสด
        <br />
        <span class="text-secondary">น่ารักพอดีคำ</span>
        <br />
        <span class="text-text/80 text-2xl sm:text-3xl md:text-4xl lg:text-5xl">สำหรับทุกช่วงพักประชุม</span>
      </h1>

      <!-- Subtitle -->
      <p class="text-lg md:text-xl text-gray-500 mb-8 animate-fade-in-up" style="animation-delay: 0.1s">
        <span class="font-medium text-primary">Kanom Kru Kung</span> — ขนมครูกุ้ง
      </p>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-12 animate-fade-in-up" style="animation-delay: 0.2s">
        <a href="#packages" class="btn-primary w-full sm:w-auto">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
          </svg>
          ดูชุดเบรคประชุม
        </a>
        <a href="#products" class="btn-outline w-full sm:w-auto">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z" />
          </svg>
          ดูขนมขายดี
        </a>
      </div>

      <!-- Trust badges -->
      <div class="flex flex-wrap items-center justify-center gap-4 md:gap-8 animate-fade-in-up" style="animation-delay: 0.3s">
        {trustBadges.map((badge) => (
          <div class="flex items-center gap-2 px-4 py-2 bg-white/60 rounded-full border border-primary/10 shadow-soft">
            <span class="text-lg">{badge.icon}</span>
            <span class="text-sm font-medium text-text/80">{badge.text}</span>
          </div>
        ))}
      </div>
    </div>

    <!-- Scroll indicator -->
    <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce hidden md:block">
      <a href="#products" aria-label="เลื่อนลงดูเพิ่มเติม" class="block p-2 text-primary/50 hover:text-primary transition-colors">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
        </svg>
      </a>
    </div>
  </div>
</section>
